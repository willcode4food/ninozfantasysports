// import WithSession from 'providers/WithSession'
// import { render, cleanup } from '@testing-library/react'
import '@testing-library/jest-dom'
// import AccountSecurityForm from './'

// let useContextMock
// Setup mock
// beforeEach(() => {
//     useContextMock = React.useContext = jest.fn()
// })

// afterEach(cleanup)

describe('AccountSecurityForm', () => {
    // useContextMock = React.useContext = jest.fn()
    // useContextMock.mockReturnValue({ email: 'hsimpson@springfieldpower.com', uid: '234234234' })
    // it('should render the basic fields', () => {
    //     render(<AccountSecurityForm />)
    //     expect(0).toBe(0)
    //     // expect(screen.getByRole('heading', { name: 'Email and Password' })).toBeInTheDocument()
    // })
})
